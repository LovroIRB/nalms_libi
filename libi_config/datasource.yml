apiVersion: 1

datasources:
  - name: tdt1
    type: elasticsearch
    access: proxy
    database: "tdt1_*"
    url: http://elasticsearch:9200
    jsonData:
      esVersion: 60
      timeField: "message_time"
      logMessageField: "id"
